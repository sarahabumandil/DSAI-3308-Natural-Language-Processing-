Every natural language processing tool has to be evaluated. And language models have to be evaluated as well. And what does it mean for to our language model to be a good language model? In general, we say a good language model is one that is better at finding the good sentences and predict and liking them more than the bad sentences. Or more specifically, we want to assign a higher probability to real or perhaps frequently observed sentences than ungrammatical or impossible, or at least a rarely observed sentences. So that's the goal of our evaluating language model. Now we train the parameters of a language model on a training set. And we test the model's performance on data that we haven't seen. So we have some training data and some unseen data, this unseen data is called a test set. And we want it to be something that's not the same as our training set totally unused. We've never looked at it before. And that will be a fair evaluation of our model. And then we'll need an evaluation metric that tells you how well does your model do on this unseen test set? So what are these evaluation models? The best evaluation, best way of comparing any two models to language models, A and B is to put each model in a task. So we're going to build our spelling corrector or speech recognizer or MP system, whatever our application is that uses language models. We'll put our language model in there and now we'll run our task and we'll get some accuracy for the system running with Model A. The system running with Model B. Perhaps that's how many misspelled words are corrected properly if we're doing spelling correction or how many words are translated correctly if we're doing translation. And now we just compare the accuracy of the two models. Whichever model has a higher accuracy is the better language model. So this is called extrinsic evaluation. We're using something external to the engram model itself. And looking at our performance on that external task, the problem with this kind of extrinsic evaluation is also called InVivo evaluation is that it's time consuming. In many cases, this can take days or weeks for a modern machine translation system or modern speech recognition system. Running evaluations can often be extremely slow. So instead, what we sometimes use is an intrinsic evaluation is something that's about intrinsically, about language models themselves and not about any particular application. And the most common intrinsic evaluation is called perplexity. Now, perplexity happens to be a bad approximation to an extricates extrinsic evaluation unless it turns out that the test data looks a lot like the training data. So generally, perplexity is useful only in pilot experiments, but it does help to think about the problem. And it's a useful tool as long as we also use extrinsic evaluation as well. So let's think about the intuition, that perplexity and like many ideas in language modeling. And this dates back to Claude Shannon. So show Shannon proposed, among many other things, a game about word prediction. How well can we predict the next word? So, for example, we've seen sentences like I always order pizza with cheese. And our job is to predict the next word. So from this first sentence, we might say, well, a good language model might guess that we are likely to have mushrooms and likely to have pepperoni and maybe less likely to have anchovies because anchovies are somewhat less popular than mushrooms and very unlikely to put fried rice on our pizza and extremely unlikely, let's say, to have and and other people, I guess, do say in the end after after the word end. And so the how well the model predicts the the actual words that occur is is the into is how good the model is. So a model on a cent on a sentence like the thirty third president of the U.S., we know the next word is very likely to be JFK or John or Kennedy or some word like that. So this is a pretty predictable case. Here we have I saw anything could come next. So in some cases we're gonna do have b b much better predict the next word, in some cases very much worse. But a good language model on average should do better than a bad language model. Now, it turns out that, you know, grams are very bad at this game. And if you think for a second you'll realize why. So in summary, a better model of text, a better language model is one that assigns a higher probability, assigns a higher probability to whatever word actually occurs. If you can guess right the next word, you are a good language model. So the best language model is one that best predicts an unseen test set or assigns on average the highest probability of a sentence to all the sentences that it sees. If I've seen this test, if I see this test set and I assign you give me a new test set and I assign a probability to each of those sentences, the better language model is the one that says, oh, I knew that sentence was coming and assigns it a very high probability. Now, the perplexity, this new metric that we're gonna be using is the probability of the test set normalized by the number of words. So we'll take let's say our test set is a long sentence and and words long. So we'll take this word sentence. We'll take its probability and we'll take the one over and we'll take the enth the end throughout. So and we'll take the inverse of it. So it's it's a way of normalizing for the length of the probability. So it's it's take this long sentence, take the probably the whole sentence and normalize by the number of words, because obviously long sentences, the longer the sentence, the less probable it's going gonna be. We want some normalizing factor so we can compare test sets of different length. So that's the end through it of one over the perplexity of a string of words. W is the end throughout of one over the probability of the string of words of this parentheses should be here. So and by the chain rule. That's the the probability of this string of words, one through N is the probability overall, I sorry. I'm sorry. The product. Overall I of the probability of each word, given the entire prefix beforehand. And so we've just buy the chain rule replaced the probability of a long sequence with the product of the probabilities of each word, given its prefix. And then for bigram is by our mark of approximation to the chain rule. We can say that the probability we've replaced the probability of a sequence of words with the product of a bunch of by grams. So the perplexity of a string of words is the end throughout of the product of of an bigram probabilities multiplied together and inverted. So it's just a function of the probability of the sentence. So because of this inversion, minimizing perplexity is the same as maximizing probability. There's another intuition for perplexity also based on Shannon. And and this example comes from Josh Goodman. And this is the second intuition for perplexity relies on the idea that perplexity is the average related to the average branching factor. Perplexity at any point in a in a sentence is on average, how many things could occur next? And we'll see later. This is related to the probability of the upcoming things related to the entropy of the upcoming things. But roughly speaking, if I had 10 possible words that could come next and they were all equal probability, my perplexity would be 10. So, for example, if I'm recognizing the ten digits, then the perplexity of the task is tenuous. There's ten possible things that could come next, and I can't decide between them. If I have to report to recognize if I'm building a speech recognizer for a for a switchboard phone service and I have to recognize 30000 names, then the perplexity of the names is thirty thousand if they're all equally likely. But suppose a system has to represent, has to recognize. Let's say again, a phone switchboard, phone operator, automatic phone operator has to recognize the word operator. And that occurs a quarter of the time. The words sales that occurs a quarter of the time or the word technical support that occurs a quarter of the time. And then with one over one hundred and twenty thousand times each. Another 30000 names occur. So now we have to take the weighted average of all these possibilities of what could occur to compute on average. How likely is any one word to occur? And now the perplexity is 54. So the perplexity, again, is the weighted equivalent branch, in fact, let's see the intuition for that last number. Let's suppose I call routing system gets one hundred and twenty thousand calls. So we the sequence of one hundred twenty thousand calls. And again, we're assuming that operator occurs when four times sales one four times technical support one in four times and the other thirty thousand times we're getting thirty thousand different names. What's the perplexity of this sequence of length. One hundred and twenty K. So to get the perplexity we're going to multiply one hundred and twenty thousand probabilities. Ninety thousand of these are one out of four. That's all of these. And thirty thousand of these are one out of one hundred and twenty thousand. And then we're gonna take the inverse. Hundred and twenty thousands route. Just like any other sequence of text of length. One hundred and twenty thousand. So the perplexity is a quarter times a quarter times a quarter times a quarter. Ninety thousand times. Times one pver one twenty thousand one. Over one hundred and twenty thousand. Thirty thousand times. And then we take the one over hundred twenty thousandth root of that. And we can simplify this arithmetically to just a N equals four case where we have operator with probability. One over four, sales one fourth, tech support, one fourth, and thirty thousand names each with a one over a hundred and twenty thousand probability. So that's, a quarter of the time, we get this, a quarter of the time we get this, quarter of the time we get this, a quarter of the time we get this. And then we take the negative one over fourth root. And that's your fifty two point six perplexity. So let's examine this new kind of perplexity, the weighted equivalent branching factor, and show that it's the same as this inverted normalized probability metric. So let's take a sentence consisting of random digits. What's the perplexity of this sentence? According to a model that assigns per equal probability to each digit. So we'll see the perplexity of the sentence, the string of of digits. Let's make it. Let's make it. Sen. Doesn't matter how long it is. So we have a bunch of digits. And the probability of this bunch of digits, we'll call them digit one digit to three digits in the perplexingly by our first metric is negative. One is the probability of this sequence to the negative one over in. And since we've said that each of these words has probability one 10, one tenth. And we're assuming a unique grand probability. So that's probability of one tenth times, one tenth times, one tenth times one tenth and so on. So that's the problem. That's one tenth to the end because there's the there were N words to the negative one over N. And as if we can see over here, that's equal to the ends. Cancel, we get one tenth the minus one or we get ten. So by thinking about perplexity as the normalized probability of a long string, we can sort of see the intuition that the average branching factor by normalizing for the length, we're sort of asking how many things can occur with each time weighted by their probability. All right. So now perplexity in general, the lower the perplexity, the better of the model. So, for example, here's here's where a training set trained on thirty eight million words tested on one point five million words from the newspaper, The Wall Street Journal. And a youna grand model has a perplexity of nine hundred and sixty to a Bigram model, has a much lower, much more accurate perplexity of one hundred and seventy. And a Trigram model has even a lower perplexity. So perplexity since it's modeling something like average branching factor or average predictability, the lower you get, the better you are predicting the the actual data that occurs.
